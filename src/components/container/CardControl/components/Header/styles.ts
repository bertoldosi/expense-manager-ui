import styled from "styled-components";

export const Scontent = styled.div`
  display: flex;
  justify-content: end;
  gap: 1rem;
  margin-bottom: 4rem;

  button {
    padding: 1rem 2rem;
    border: none;
    outline: none;

    border-radius: 0.3rem;
  }
`;
