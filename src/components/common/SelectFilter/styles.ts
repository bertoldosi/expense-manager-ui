import styled from "styled-components";

export const Scontent = styled.div`
  select {
    border: none;
    background: #fff;
    border: 1px solid #3333;
    padding: 0.2rem;
    color: #333;
    cursor: pointer;
  }
`;
